# Rendering artwork

Pure lit-html SVG templates for World entities and deterministic preparation of
outdoor floor artwork. Its external interface is the entity and terrain
template functions; it observes World data without owning rendering lifecycle
or domain transitions. Tests are colocated in `artwork/__tests__/`.
